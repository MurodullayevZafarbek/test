# test

### Clone qilish
git clone url .

### branch ko`rish
git branch

### branch yasash
git branch <nomi>

### branch o`zgartirish
git checkout <nomi>

### qo`shish
git add .
git commit -m"commnet"

git push --set-upstream origin <branch nomi>
